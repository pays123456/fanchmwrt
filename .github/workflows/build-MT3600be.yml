name: Build GL-MT3600BE (Final Local DTS)

on:
  workflow_dispatch:

env:
  FANCHM_REPO: https://github.com/pays123456/fanchmwrt
  FANCHM_BRANCH: fanchmwrt-25.12.4

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 240

    steps:
      - name: 🧹 释放磁盘空间
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
          sudo apt-get clean
          df -h

      - name: 📦 安装编译依赖
        run: |
          sudo apt update -y
          sudo apt install -y build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git libncurses-dev libssl-dev \
            python3-setuptools rsync swig unzip zlib1g-dev file wget curl \
            ccache libelf-dev device-tree-compiler qemu-utils xsltproc \
            python3-distutils

      - name: 🔄 克隆 FanchmWrt
        run: |
          git clone --depth=1 -b ${{ env.FANCHM_BRANCH }} ${{ env.FANCHM_REPO }} fanchmwrt
          cd fanchmwrt
          echo "FanchmWrt Branch: $(git branch --show-current)"
          
          # 1. 验证 DTS 文件是否存在
          DTS_PATH="target/linux/mediatek/dts/mt7987a-glinet-gl-mt3600be.dts"
          if [ ! -f "$DTS_PATH" ]; then
            echo "❌ ERROR: DTS file not found at $DTS_PATH"
            exit 1
          fi
          echo "✅ DTS file verified."

      - name: 🔧 自动注入设备定义
        run: |
          cd fanchmwrt
          
          MK_FILE="target/linux/mediatek/image/filogic.mk"
          MARKER="# --- AUTO-INJECTED MT3600BE ---"

          # 如果还没注入，则追加
          if ! grep -q "$MARKER" "$MK_FILE"; then
            echo "Injecting device definition into $MK_FILE ..."
            
            cat >> "$MK_FILE" <<'EOF'

# --- AUTO-INJECTED MT3600BE ---
define Device/glinet_gl-mt3600be
  DEVICE_VENDOR := GL.iNet
  DEVICE_MODEL := GL-MT3600BE
  DEVICE_DTS := mt7987a-glinet-gl-mt3600be
  DEVICE_DTS_DIR := ../dts
  DEVICE_PACKAGES := kmod-mt7996-firmware uboot-envtools
  BLOCKSIZE := 128k
  PAGESIZE := 2048
  KERNEL_IN_UBI := 1
  UBINIZE_OPTS := -E 5
  IMAGE/sysupgrade.bin := sysupgrade-tar | append-metadata
endef
TARGET_DEVICES += glinet_gl-mt3600be
# --- END AUTO-INJECTED MT3600BE ---
EOF
            echo "✅ Definition injected."
          else
            echo "⚠️ Definition already exists, skipping."
          fi

      - name: ⚙️ 配置与编译
        run: |
          cd fanchmwrt
          
          # 更新 feeds
          ./scripts/feeds update -a
          ./scripts/feeds install -a

          # 设备配置项
          CONFIG_ITEM="CONFIG_TARGET_mediatek_filogic_DEVICE_glinet_gl-mt3600be"
          
          # 创建初始配置
          cat > .config <<EOF
          CONFIG_HAVE_DOT_CONFIG=y
          CONFIG_TARGET_mediatek=y
          CONFIG_TARGET_mediatek_filogic=y
          ${CONFIG_ITEM}=y
          EOF

          echo "Running initial defconfig..."
          make defconfig

          # 验证设备是否被识别
          if grep -q "${CONFIG_ITEM}=y" .config; then
            echo "✅ Device successfully recognized in config."
          else
            echo "❌ ERROR: Device not found in config after defconfig."
            exit 1
          fi

          # 添加额外包和功能
          {
            echo "CONFIG_PACKAGE_luci-app-oaf=y"
            echo "CONFIG_PACKAGE_oaf=y"
            echo "CONFIG_LUCI_LANG_zh_Hans=y"
            echo "CONFIG_PACKAGE_kmod-mt7996-firmware=y"
            echo "CONFIG_PACKAGE_wpad-openssl=y"
          } >> .config

          echo "Running final defconfig..."
          make defconfig

          echo "🚀 Starting compilation..."
          make -j$(nproc) download
          make -j$(nproc) || make -j1 V=s

      - name: 📤 上传固件
        uses: actions/upload-artifact@v4
        with:
          name: GL-MT3600BE-Final-Build-${{ github.run_number }}
          path: |
            fanchmwrt/bin/targets/mediatek/filogic/*sysupgrade*
            fanchmwrt/bin/targets/mediatek/filogic/*.manifest
          retention-days: 7
